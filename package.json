{
  "name": "vbl-pro-ts",
  "version": "1.4.4",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "winput": "latest",
    "smol-toml": "^1.5.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "prettier": "bun run scripts.ts --prettier",
    "build": "bun run scripts.ts --build --run",
    "compile": "bun run scripts.ts --compile --run",
    "commit": "bun run scripts.ts --prettier --commit",
    "release": "bun run scripts.ts --prettier --commit --compile --release",
    "start": "bun run scripts.ts --run"
  },
  "type": "module"
}
